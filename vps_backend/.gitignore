# Virtualenv & cache (dibuat ulang di VPS lewat setup.ps1)
*/venv/
**/__pycache__/
*.pyc

# Log runtime
brain/logs/
executor/logs/

# Setelan bot yang diubah lewat aplikasi (state runtime per-VPS, bukan kode).
# Default pabriknya ada di executor/config.py.
executor/bot_settings.json
executor/bot_settings.json.tmp
